1.css导入图片路径相对于当前css文件；
2.transform：rotate属性指相对与图片原始位置旋转，而不是当前位置；
3.为icon设置inline-block；
4.增加height值，以遮住下border；
5.大横幅以背景形式插入；
？6.问题调试时候display none的怎么在浏览器一直显示(让jquery失效)；
7.jquery index()不传jq对象默认当前对象在兄弟元素位置；